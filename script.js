window.onload = function() {
    let birthdayMessage = document.getElementById('birthdayMessage');
    birthdayMessage.style.opacity = 1; // Hace que el mensaje aparezca
};window.onload = function() {
    let birthdayMessage = document.getElementById('birthdayMessage');
    birthdayMessage.style.opacity = 1; // Hace que el mensaje aparezca
}